# Zigbang Prototype

업로드된 모바일 화면을 기준으로 만든 정적 프로토타입입니다.

## 실행

`index.html`을 브라우저에서 열면 바로 확인할 수 있습니다.

## 배포

GitHub Pages에서 배포할 때는 repository Settings → Pages에서 branch를 `main`, folder를 `/root`로 선택하면 됩니다.
